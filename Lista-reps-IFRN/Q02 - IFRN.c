/* Escreva um algoritmo que calcule a soma dos números de 1 a 15.*/

#include <stdio.h>
#include <locale.h>
#include <math.h>

int main(){
	setlocale(LC_ALL, "");
	int i;
	for (i=0; i<15; i++){
		int v1, res;
		res = v1 * 2;
		printf("%d somado a %d é igual a %d\n", v1, v1, res);
		v1++;
		
	}
}
